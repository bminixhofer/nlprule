// use nlprule::tokenizer::{tokenize, DISAMBIGUATOR};

fn main() {
    // env_logger::init();
    // let text = std::env::args().nth(1).unwrap();

    // let tokens = tokenize(&text);
    // let tokens = DISAMBIGUATOR.apply(tokens);

    // println!("{:#?}", tokens);
}
